# 2026-06-04 修正紀錄

## 版本

- 版本號更新為 `1.8`。

## 型態分類

- 型態判定優先順序改為 `C > A > B`。
- 符合型態 C 的個股，即使同時符合型態 A 或型態 B，也會優先歸類為 C。
- 前端選單文案由「回撤反彈（例外C）」改為「回撤反彈（型態C）」。

## 型態 A

- MA10 / MA20 糾結條件由「最新一天差距 <= 3%」改為「最近 10 個交易日每日差距都 <= 3%」。
- 判斷式：

```text
近 10 日每日 abs(MA10 - MA20) / MA20 <= 3%
```

## 型態 C

- 觀察期由最近 90 根 K 線改為最近 65 根 K 線。
- 回撤幅度由 18%~36% 改為 16%~36%。
- 新增「反彈不過前高」條件。
- 目前型態 C 條件摘要：

```text
近 65 根 K 線內找前高
(前高 - 前低) / 前低 >= 50%
16% <= (前高 - 回撤低點) / 前高 <= 36%
(回撤後最高價 - 回撤低點) / 回撤低點 >= 8%
回撤後最高價 <= 前高
後續最低價 >= 回撤低點
```

## 領漲 / 領跌族群

- 確認領漲族群與領跌族群使用 `stock_concepts.json` 中的全市場主題資料。
- 主題資料由 `update_stock_concepts.py` 從 `https://twstockflow.aihost.dev` 抓取。
- 前端族群卡片顯示改為「市場 N 檔」，避免誤解為目前篩選結果中的檔數。

## 資料更新

- 已依上述條件重新產生 `uptrend_results.json`。
- 最新結果時間：`2026-06-04 15:08:54`。
- 最終符合條件個股數：`388`。

## 驗證

- `python -m py_compile app.py update_data.py` 通過。
- 本地 Streamlit `http://localhost:8501` 回應 `200 OK`。
