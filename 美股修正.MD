# 美股修正：新增型態 C

## 目標

將台股版目前的「型態 C：回撤反彈」移植到美股版 `scan_US`。

型態 C 用來補捉已經大漲過、從高點合理回撤後重新反彈的標的。它不走原本 A/B 的底部墊高共同條件，而是獨立規則。

## 型態 C 條件

使用最近 65 根日 K：

1. 近 65 日內的最高價，不能是最後 2 根 K 棒。
2. 該 65 日高點，必須比高點前的最低價高出 50% 以上。
3. 高點後必須出現 16% 到 36% 的回撤。
4. 回撤低點之後，最高價必須反彈 8% 以上。
5. 回撤後最高價不能突破前高。
6. 回撤低點之後，股價不能再跌破該回撤低點。

## `update_data.py` 修改

### 1. 新增型態 C 函式

在美股版 `update_data.py` 裡，放在 `check_type_a` / `check_type_b` 附近：

```python
def check_type_c_pullback_rebound(df):
    if len(df) < 65:
        return False

    recent = df.tail(65).copy()
    high_pos = int(np.argmax(recent['High'].to_numpy()))
    high_price = float(recent['High'].iloc[high_pos])
    if high_price <= 0 or high_pos >= len(recent) - 2:
        return False

    prior_low = float(recent['Low'].iloc[:high_pos + 1].min())
    if prior_low <= 0 or (high_price - prior_low) / prior_low < 0.50:
        return False

    after_high = recent.iloc[high_pos + 1:].copy()
    for low_pos_rel in range(len(after_high) - 1):
        low_price = float(after_high['Low'].iloc[low_pos_rel])
        if low_price <= 0:
            continue

        drawdown = (high_price - low_price) / high_price
        if drawdown < 0.16 or drawdown > 0.36:
            continue

        after_low = after_high.iloc[low_pos_rel:].copy()
        rebound_high = float(after_low['High'].max())
        if rebound_high > high_price:
            continue

        rebound = (rebound_high - low_price) / low_price
        if rebound < 0.08:
            continue

        lows_after_pullback = after_low['Low'].iloc[1:]
        if len(lows_after_pullback) and float(lows_after_pullback.min()) < low_price:
            continue

        return True

    return False
```

注意：美股版若尚未 `import numpy as np`，要在檔案上方補上：

```python
import numpy as np
```

### 2. 主篩選流程加入 C

原本美股版大致會有：

```python
is_a = check_type_a(clean_df)
is_b = check_type_b(clean_df)
if not (is_a or is_b):
    filtered_out_by_ma += 1
    continue
stock_type = 'A' if is_a else 'B'
```

改成：

```python
is_a = check_type_a(clean_df)
is_b = check_type_b(clean_df)
is_c = check_type_c_pullback_rebound(clean_df)
if not (is_a or is_b or is_c):
    filtered_out_by_ma += 1
    continue
stock_type = 'C' if is_c else ('A' if is_a else 'B')
```

這裡使用新版優先順序：

1. C 優先
2. A 次之
3. B 最後

也就是一檔股票同時符合 A/B/C 時，優先標示為 C。

### 3. 輸出資料保留 `type`

美股版輸出的每檔資料裡應該已經有：

```python
'type': stock_type,
```

若沒有，要補上，讓 `app.py` 可以顯示 A/B/C。

## `app.py` 修改

### 1. 型態篩選選項新增 C

如果美股版目前只有：

```python
type_options = {
    '': 'All',
    'A': 'Pullback Base',
    'B': 'MA Bullish',
}
```

新增：

```python
'C': 'Pullback Rebound',
```

或依目前美股版中文/英文 UI 統一命名，例如：

```python
type_options = {
    '': 'All',
    'A': 'Post-Rally Base',
    'B': 'Bullish MA Stack',
    'C': 'Pullback Rebound',
}
```

### 2. 股票標題型態名稱新增 C

如果有類似：

```python
type_text = {
    'A': 'Post-Rally Base',
    'B': 'Bullish MA Stack',
}.get(k_data.get('type'), 'Bullish MA Stack')
```

改成：

```python
type_text = {
    'A': 'Post-Rally Base',
    'B': 'Bullish MA Stack',
    'C': 'Pullback Rebound',
}.get(k_data.get('type'), 'Bullish MA Stack')
```

### 3. 分頁連結要保留 `type=C`

確認 `build_page_href()` 或分頁網址產生函式有保留目前的 `type` query param。

若美股版已修過分頁跳轉保持型態篩選，通常不需要再改；只要 C 加進選項即可。

## 文件修改

建議同步更新美股版文件：

1. `篩股邏輯.md` 或美股版等效文件
2. `README.md` 或部署說明
3. 若有 `scan_US_修改重點...md`，記錄新增型態 C

型態 A 若美股版仍是只看最新一天 MA10/MA20 差距，也要同步改為最近 10 個交易日每日差距都 <= 3%。

型態 C 文件可寫：

```markdown
### 型態 C：Pullback Rebound

- 最近 65 日內創高。
- 該高點必須比高點前低點高出 50% 以上。
- 高點後回撤 16%-36%。
- 回撤低點後反彈 8% 以上。
- 回撤後最高價不能突破前高。
- 反彈後不得再跌破該回撤低點。
- 通過型態後仍需符合趨勢段與量能條件。
```

## 更新資料與驗證

在 `scan_US` 專案目錄執行：

```bash
python -m py_compile app.py update_data.py
python update_data.py
```

檢查 `uptrend_results.json`：

```bash
python -c "import json; d=json.load(open('uptrend_results.json',encoding='utf-8')); print(d.get('last_updated'), d.get('total_symbols_found')); print(sum(1 for v in d['results'].values() if v.get('type')=='C'))"
```

若數量合理，再提交：

```bash
git add app.py update_data.py uptrend_results.json 篩股邏輯.md
git commit -m "Add type C pullback rebound screen"
git push
```

## 注意事項

- C 是獨立規則，不要套用 A/B 的 `_check_base()`，否則會失去「回撤後反彈」的意義。
- C 通過後仍要保留美股版原本的趨勢段條件、量能條件、RS/SPY 或其他美股專屬條件。
- 若美股版有 ETF / ADR / low-price 排除規則，不需要因 C 放寬。
- 若美股版輸出 TradingView 清單已按產業分類，型態 C 不需要改匯出格式。
