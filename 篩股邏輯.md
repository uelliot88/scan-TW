# 台股掃圖 — 篩股邏輯說明

## 第一重：型態濾網

個股只要符合型態 A、型態 B、型態 C 任一種，就會進入下一重篩選。

型態判定優先順序：

```text
A 優先，其次 B，最後 C
```

也就是同時符合多個型態時，輸出型態會依序標為 A、B、C。

---

## 型態 A — 漲後整理

型態 A 使用 A/B 共用基底條件，並額外要求均線糾結。

### A/B 共用基底條件

| 條件 | 說明 |
|------|------|
| 資料足夠 | 至少 120 根 K 線 |
| 底部墊高 | 最新低點 > 前 120～90 日最高點；近 30 日低點 >= 前 60～90 日低點；前 60 日低點 >= 前 90～120 日低點 |
| 季線壓制排除 | 近半年收低於 MA60 次數 <= 60 次 |
| MA10/MA20 同站 MA60 比例 | 近半年內有 >= 25% 的交易日，MA10 與 MA20 均在 MA60 之上 |
| 收盤站上季線 | 最新收盤價 > MA60 |

### A 型態額外條件

```text
abs(MA10 - MA20) / MA20 <= 3%
```

---

## 型態 B — 多頭排列

型態 B 同樣使用 A/B 共用基底條件，並額外要求均線多頭排列。

### B 型態額外條件

```text
MA10 > MA20 > MA60
```

---

## 型態 C — 回撤反彈例外型態

型態 C 不使用 A/B 的均線基底條件，而是使用近 90 根 K 線的高點、回撤與反彈條件。

### C-1：資料與高點

| 條件 | 說明 |
|------|------|
| 資料足夠 | 至少 90 根 K 線 |
| 觀察範圍 | 最近 90 根 K 線 |
| 高點位置 | 找出 90 根 K 線內最高價 |
| 高點不能太靠近最新日 | 高點不能出現在最後 2 根 K 線內，因為後續必須要有回撤與反彈資料 |

### C-2：高點前漲幅

找出高點之前到高點當下的最低價。

高點相對該前低必須上漲至少 50%。

```text
(高點 - 高點前最低價) / 高點前最低價 >= 50%
```

### C-3：高點後回撤

高點後的 K 線中，尋找一個回撤低點。

該低點相對高點的回撤幅度必須介於 18% 到 36%。

```text
18% <= (高點 - 回撤低點) / 高點 <= 36%
```

### C-4：回撤後反彈

從該回撤低點之後的 K 線中找最高價。

反彈幅度必須至少 8%。

```text
(回撤後最高價 - 回撤低點) / 回撤低點 >= 8%
```

### C-5：不得再破回撤低點

回撤低點之後，後續 K 線最低價不能跌破該回撤低點。

```text
回撤低點之後的最低價 >= 回撤低點
```

### C 型態通過方式

只要高點後存在一個回撤低點同時滿足：

```text
回撤 18%~36%
反彈 >= 8%
後續不破該回撤低點
```

即可視為型態 C 通過。

---

## 第二重：波段識別

通過第一重型態濾網後，仍需找到至少一段有效上漲波段。

有效波段條件：

| 條件 | 設定 |
|------|------|
| 最小漲幅 | >= 20% |
| 最短持續天數 | >= 5 日 |
| 波段純度 | 波段期間低點不可跌破起漲點 |
| 波峰波谷識別 | 使用局部高低點演算法，視窗為前後各 15 日 |

---

## 第三重：流動性

通過型態與波段後，仍需符合成交量條件。

```text
近 10 日均量 >= 800 張
```

換算股數：

```text
近 10 日均量 >= 800,000 股
```

---

## 標注（不篩選，僅顯示）

| 標注 | 條件 |
|------|------|
| 🔵 外資 | 近 2 日連續買超 + 近 5 日淨額 > 0 |
| 🟢 投信 | 近 2 日連續買超 + 近 5 日淨額 > 0 |
| VOL🔺 | 近 5 日均量 >= 近 20 日均量 × 1.6 |

---

## 資料設定

| 項目 | 設定值 |
|------|--------|
| 股票 universe | TWSE/TPEX ISIN 清單中的股票與創新板股票；排除 ETF/ETN/權證等非股票區段 |
| 下載範圍 | 近 1.2 年 |
| 圖表顯示 | 最近 200 根 K 線 |
| 法人回溯 | 近 7 個交易日 |
| 最小成交量 | 近 10 日均量 >= 800 張 |
| 最小波段漲幅 | >= 20% |
| 最短波段天數 | >= 5 日 |
| C 型態觀察期 | 最近 90 根 K 線 |

---

## 程式位置

| 邏輯 | 位置 |
|------|------|
| A/B 共用基底 | `update_data.py` 的 `_check_base()` |
| 型態 A | `update_data.py` 的 `check_type_a()` |
| 型態 B | `update_data.py` 的 `check_type_b()` |
| 型態 C | `update_data.py` 的 `check_type_c_pullback_rebound()` |
| 波段識別 | `update_data.py` 的 `identify_uptrend()` |
| 成交量篩選 | `update_data.py` 的 `check_volume()` |
| 前端型態篩選與顯示 | `app.py` |
