# C 型態選股邏輯

## 定義

C 型態是「回撤反彈」例外型態，用來捕捉近期強勢創高後，經過合理回撤並重新反彈的標的。

## 第一重：C 型態條件

以最近 90 根 K 線為判斷範圍。

1. 資料至少要有 90 根 K 線。
2. 在最近 90 根 K 線內找出最高價。
3. 最高價不能出現在最後 2 根 K 線內，因為後續必須要有回撤與反彈資料。
4. 找出該高點之前到高點當下的最低價。
5. 高點相對前低漲幅必須至少 50%。

計算方式：

```text
(高點 - 高點前最低價) / 高點前最低價 >= 50%
```

## 第二重：高點後回撤

從高點之後的 K 線中找出最低價，作為回撤低點。

高點到回撤低點的跌幅必須介於 18% 到 36%。

計算方式：

```text
(高點 - 回撤低點) / 高點
```

通過區間：

```text
18% <= 回撤幅度 <= 36%
```

## 第三重：回撤後反彈

從回撤低點之後的 K 線中找出最高價。

該最高價相對回撤低點必須至少反彈 6%。

計算方式：

```text
(回撤後最高價 - 回撤低點) / 回撤低點 >= 6%
```

## 第四重：不得再破回撤低點

回撤低點之後，後續 K 線的最低價不能跌破該回撤低點。

也就是：

```text
回撤低點之後的最低價 >= 回撤低點
```

## 後續共同條件

C 型態通過後，仍要繼續通過原本的共同篩選條件：

1. 波段識別：至少要有一段 >= 20% 的有效波段。
2. 成交量條件：近 10 日均量需達最低成交量門檻。

## 顯示方式

在前端頁面中：

- 型態篩選名稱：`回撤反彈（例外C）`
- 個股標題顯示：`｜回撤反彈`

## 程式位置

- C 型態判斷：`update_data.py` 的 `check_type_c_pullback_rebound()`
- 型態套用：`update_data.py` 主流程中的 `stock_type = 'A' if is_a else ('B' if is_b else 'C')`
- 前端篩選與顯示：`app.py`
